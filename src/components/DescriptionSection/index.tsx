import { DescriptionSectionProps } from "./@types";
import * as S from './style'

export function DescriptionSection({ children }: DescriptionSectionProps) {
    return <S.DescriptionSection>{children}</S.DescriptionSection>
}